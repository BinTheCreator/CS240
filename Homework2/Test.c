#include<stdio.h>

int main() {
    int i = 2; //being used to assign a variable 

    if (1 = i) //being used as an equality operator
    { 
        printf("true\n");
    } else {
        printf("false\n");
    }
   
}